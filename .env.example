# Copy this to .env.local for local dev (optional - proxy handles /api locally)
# Required on Vercel: set as Environment Variable in project settings
VITE_API_BASE_URL=https://your-backend.onrender.com/api/v1
